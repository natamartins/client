import { render, screen } from "@testing-library/react";

import {{pascalCase name}} from ".";

describe("
<{{pascalCase name}} />", () => {
it("should render the heading", () => {
render(
<{{pascalCase name}} />);

expect(screen.getAllByRole("heading", { name: /{{pascalCase name}}/i }))
.toBeInTheDocument();
});
});
